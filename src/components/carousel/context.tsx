// "use client";
// import { MotionValue } from "framer-motion";
// import { createContext, useRef, useContext, RefObject } from "react";

// // type Value = RefObject;

// type ProviderProps = {
//   children: React.ReactNode;
// //   x: MotionValue<number>;
// //   parentRect: React.MutableRefObject<DOMRect | null>;
// };

// // const Context = createContext<>(null);

// function CarouselProvider({ children }: ProviderProps) {
    
   
  
//   // return (
//   //   <Context.Provider value={null}>{children}</Context.Provider>
//   // );
// }

// function useCarousel() {
//   const context = useContext(Context);
//   if (context === undefined) {
//     throw new Error("useCarousel must be used within a CarouselProvider");
//   }
//   return context;
// }

// export { CarouselProvider, useCarousel };
